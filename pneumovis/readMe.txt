Hello there,

To run:

Make sure you are in the folder with manage.py in it

Linux command to run server:

   python manage.py runserver

Then click the link it produces to open the webpage

You may need to install the following modules:

matlabplot
mpld3
jinja2
virtualenv
